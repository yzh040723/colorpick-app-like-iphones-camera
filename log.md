## beta 0.4.08

- 版本代码：408
- 更新时间：2026-08-25 14:37（北京时间）

### 相纸动画时序优化

- 白光结束后不再等待后台滤镜处理，CameraX 保存完成即立即启动相纸打印动画。
- 相纸从顶部缓慢移动到屏幕中央，入场时长调整为 1100ms，避免移动过快。
- 中央停留时间调整为 1 秒。
- 相纸随后以 700ms 平滑缩小并移动到左下角相册缩略图位置，完成后消失。

---

## beta 0.4.07

- 版本代码：407
- 更新时间：2026-08-25 14:09（北京时间）

### 相纸与滤镜模式修复

- 相纸尺寸增大为 280 × 366dp，保留白色相纸边框、阴影和底部留白，增强拍立得打印成片质感。
- 相纸从顶部落入屏幕中央，停留 2 秒后缩小移动到左下角相册缩略图位置并消失。
- 修复滤镜/调色盘更新时 Compose 重组覆盖 CameraX 分析器旋转状态，避免预览画面翻转和卡顿。
- 拍摄相纸改为在后台滤镜处理完成后使用最终成片 URI，滤镜模式下不再显示普通模式原图。

---

## beta 0.4.06

- 版本代码：406
- 更新时间：2026-08-25 13:43（北京时间）

### 拍立得式拍摄反馈

- 快门触发时新增中心自动对焦动作与对焦框反馈。
- 快门声音、触觉反馈与暖白色短闪同步触发，不再使用纯白闪光。
- 拍摄完成后显示带白色相纸边框和阴影的成片预览。
- 相纸从屏幕上方落下，随后缩小并移动至左下角相册缩略图位置，最后淡出消失。
- 预览使用当前拍摄照片 URI 的缩略图，不改变原有拍照、滤镜、保存与相册逻辑。`n- 相纸动画调整时间：2026-08-25 14:08；本次调整：2026-08-25 13:54；相纸放大并增强打印感，入场后居中停留 2 秒，再缩小移动到左下角缩略图位置并消失。

### 工程验证

- git diff --check 通过。
- Gradle 构建仍需在本机修复 native-platform.dll 环境问题后验证。`n- 编译修复时间：2026-08-25 13:50；修复相纸动画回调中 Composable 参数遮蔽外层可变状态导致的 `val cannot be reassigned`。

---

## beta 0.4.05

- 版本代码：405
- 更新日期：2026-08-25 10:37（北京时间）
- 热修复与回滚记录：2026-08-25 10:39–10:42

### 实时预览与反馈优化

- 保持实时预览目标分辨率 1080 × 1440。
- 优化过 GL handle 缓存、纹理上传和缓冲管理；因设备兼容性问题已回滚 GPU Renderer 改动，恢复已知可运行实现。
- 拍摄完成后立即更新界面反馈，照片后处理继续在后台执行。
- 增加拍摄快闪反馈。

---

# 拾色（ColorPick）更新日志

> 说明：所有版本更新统一记录在本文件。新增版本时，在文件最上方追加新的二级标题 `## beta x.x.x`，保持最新版本在前。

---

## beta 0.2.02

- 版本代码：201
- 更新日期：2026-08-01

### 核心改进

1. **App 正式更名「拾色」（ColorPick）**
   - 因应用市场已有同名 App「Colorby」，全项目更名以避免冲突。
   - 中文显示名「拾色」，英文名 ColorPick；桌面与应用商店图标名同步更新。

2. **全量改名（包名 + 保存目录）**
   - 包名 `com.example.colorby` → `com.example.colorpick`，源码包结构、XML 主题（`Theme.ColorPick`）、Compose 主题函数（`ColorPickTheme`）同步迁移。
   - 照片保存目录 `Pictures/ColorBy` → `Pictures/ColorPick`。
   - 拍摄页缩略图与相册扫描兼容新旧两个目录（`Pictures/ColorPick%` OR `Pictures/ColorBy%`），改名前的旧照片不丢失。
3. **App 图标重新设计**
   - 全新相机造型图标：深色圆角底座 + 亮黄描边、白色机身与取景器凸起、亮黄镜头光圈，镜头内嵌 4 个彩色调色点（呼应 11×11 点阵调色盘），保留白色镜头高光。
   - 图标文件：`app/src/main/res/drawable/ic_launcher.xml`（Manifest 中 `@drawable/ic_launcher` 同时作为 icon 与 roundIcon）。

### 工程修复

- 迁移 16 个 Kotlin 源文件至 `com/example/colorpick`，更新全部 package 声明与 import 引用。
- `AndroidManifest.xml`、`themes.xml`（含夜间主题）、`strings.xml`（app_name=拾色）资源同步改名。
- 版本号升级至 beta 0.2.02（版本代码 201）。

### 测试重点

- 卸载旧版后安装新版 APK，桌面图标与应用名显示「拾色」。
- 拍摄新照片保存于 `Pictures/ColorPick`，缩略图与相册可见；改名前的 `Pictures/ColorBy` 旧照片仍能在缩略图/相册中显示。

---

## beta 0.2.01

- 版本代码：200
- 更新日期：2026-08-01

### 核心改进

1. **相册模块上线（app 内置相册）**
   - 拍摄页左下角缩略图点击后进入 app 自带相册页，不再打开手机系统相册。
   - 相册底色为磨砂质感黑色（深黑底 + 顶部半透明黑色渐变磨砂控制条）。
   - 展示所有 app 拍摄的照片，按拍摄日期由新到旧排列。

2. **照片保存目录调整**
   - 拍摄照片与裁剪成片保存目录由 `DCIM/Camera` 改为 `Pictures/ColorBy`（软件专属文件夹），通过 MediaStore 注册，系统相册仍可见。
   - app 相册按 `Pictures/ColorBy` 目录扫描，仅展示 app 拍摄的照片。

3. **相册交互**
   - 默认宫格视图，一行 5 张，下滑自动分页加载更多（每页 30 张）。
   - 右下角圆形白色悬浮相机按钮，点击返回拍摄页面。
   - 长按图片进入多选模式：点击切换勾选，顶部显示已选数量；顶部删除按钮（确认对话框）删除选中照片（连同文件）。
   - 多选模式下顶部新增「全选 / 取消全选」按钮（针对当前展示的照片），支持在宫格上滑动连续选中/取消选中（按住未选照片拖动=连续选中，按住已选照片拖动=连续取消）。
   - 拍摄页左下角缩略图仅显示 app 拍摄（Pictures/ColorBy）的最新照片，不再展示系统相册中的照片。
   - 图片详情页后续版本再做。

### 工程修复

- 新增 `GalleryScreen.kt`：`LazyVerticalGrid` 5 列宫格 + `snapshotFlow` 分页加载 + MediaStore 查询（按 `RELATIVE_PATH` 过滤 ColorBy 目录，日期倒序）。
- `MainActivity` 新增 `gallery` 路由，`CameraScreen` 新增 `onNavigateToGallery` 回调链路（CameraContent → CameraControls → BottomControlBar → ThumbnailView）。
- `CameraScreen.kt` / `CropScreen.kt` 保存路径改为 `Pictures/ColorBy`。
- **相册扫描不到照片修复**：① 权限请求补上 `READ_MEDIA_IMAGES`（Android 13+ 查询 MediaStore 必需，此前 Manifest 已声明但未在运行时请求，导致相册查询返回空）；② 移除查询中的 SQL `LIMIT/OFFSET`（部分设备/provider 不支持会静默抛异常被吞），改为一次查询全部 + UI 端分页逐批渲染。
- 版本号升级至 beta 0.2.01（版本代码 200），开启 0.2.x 系列。

### 测试重点

- 拍摄一张照片后，缩略图出现；点击缩略图进入 app 相册，不打开系统相册。
- 拍摄/裁剪后的照片在 `Pictures/ColorBy` 目录，且系统相册（相册 App）可见。
- 相册 5 列宫格、新照片在前；照片多时下滑自动加载更多。
- 长按进入多选、勾选、删除（含确认对话框），删除后缩略图与相册同步消失。
- 右下角相机按钮与左上角返回按钮均能回到拍摄页。

---

## beta 0.1.28

- 版本代码：128
- 更新日期：2026-08-01

### 核心改进

1. **轮盘滞留问题修复（B 方案：框架托管拖拽生命周期）**
   - 手势层弃用手写 `awaitEachGesture` 事件循环，改用 `detectDragGestures`：`onDragStart`（呼出轮盘）之后框架保证必然触发 `onDragEnd` 或 `onDragCancel`（关闭轮盘），`if/else` 二分无遗漏，从机制上杜绝「抬起事件丢失 → 轮盘滞留」。
   - `detectTapGestures` 负责预设点击；本地多指监听器在第二指按下时置位，拖拽检测器据此让位，避免与双指缩放冲突，双指缩放灵敏度不受影响。
   - 变焦条拖拽呼出轮盘后松手立即恢复预设条，连续拖拽-松手循环无残留（测试通过）。

2. **预设放大倍数点击切换不再闪烁中间档位（恢复并保留）**
   - `selectedPreset` 锁定：点击预设时高亮锁定目标档位，缩放动画经过 1x 不再闪亮；拖拽/双指缩放开始即清除锁定，恢复实时跟随。

3. **轮盘指针位置调整**
   - 指针箭头整体移入圆盘内侧：尖端贴合圆边缘、箭头主体位于圆内，不再超出圆形边界。

### 工程修复

- `ZoomControl`：手势层拆分为 `detectDragGestures`（轮盘拖拽）+ `detectTapGestures`（预设点击）+ 多指监听器；新增 `selectedPreset`/`multiFingerDown` 状态。
- `ZoomPresetBar`：新增 `selectedPreset` 参数，选中判断优先使用锁定档位。
- `ZoomWheel`：指针绘制起点由圆外（`centerY - diskRadius - pointerSize`）改为圆边缘（`centerY - diskRadius`）。
- 版本号保持 beta 0.1.28（版本代码 128），并修正历史日志日期为 2026-08-01。

### 测试重点

- 变焦条单指拖拽呼出轮盘后松手：轮盘立即消失恢复预设条，无需点击；多次循环无残留。
- 双指缩放灵敏度正常（起点在变焦条上也不误开轮盘），倍数徽章松手即消失。
- 连续点击预设档位（0.6x↔2x）：中间 1x 不再闪亮，直接高亮目标档位。
- 轮盘指针尖端贴圆边缘、箭头在圆内，不超出圆形边界。

---

## beta 0.1.27（已弃用）

- 版本代码：127
- 更新日期：2026-08-01

### 核心改进

1. **轮盘滞留问题根治（手势层布局恒定）**
   - 对比双指缩放能还原而轮盘不能还原的差异：双指缩放的 pinch 手势所在取景框 Box 布局恒定，其 `finally` 必然执行；而轮盘手势层此前在 `showWheel=true` 瞬间改变自身高度/偏移（relayout），存在吞掉手指抬起事件的路径，`finally` 不触发 → 轮盘滞留。
   - 修复：手势层改为**固定全高布局**（高度恒为 `contentBaselineY`，不再随 `showWheel` 变化），布局恒定则事件流稳定，`finally` 必然执行。轮盘拖拽改为**起点位置判断**限定触发：仅当触摸起点落在变焦条区域内（或轮盘已展开时轮盘任意位置）才启动拖拽，双指缩放起点在变焦条之外不会进入手势层。
   - 保留全屏指针追踪器与取景框 pinch `finally` 作为冗余兜底，三重保障轮盘必定复原。

2. **顶部栏二级菜单上方圆角取消**
   - 六点菜单（Style/构图线）与画幅下拉菜单均改为：上方直角（贴合顶部栏边缘）、仅保留下方两个 25dp 圆角。

3. **预设放大倍数点击切换不再闪烁中间档位**
   - 原选中判断基于实时 `currentZoom`，点击 0.6x→2x（或 2x→0.6x）时缩放动画经过 1x 导致 1x 档位短暂高亮。
   - 新增 `selectedPreset` 状态：点击预设时锁定高亮到目标档位（动画期间不受中间值影响）；拖拽变焦或双指缩放开始即清除锁定，恢复实时跟随。

### 工程修复

- `ZoomControl`：手势层固定全高（无 relayout），`barTopY`/`showWheel` 用 `rememberUpdatedState` 供手势协程读取；新增 `selectedPreset` 状态并在 pinch 启动/拖拽时清除；预设点击时锁定并传给 `ZoomPresetBar`。
- `ZoomPresetBar`：新增 `selectedPreset` 参数，选中判断优先使用锁定档位。
- `SixDotMenu` / `AspectRatioDropdown`：卡片 clip 改为上方直角、下方 25dp 圆角。
- 版本号升级至 beta 0.1.27（版本代码 127）。

### 测试重点

- 变焦条单指拖拽呼出轮盘后松手：轮盘**立即**消失恢复预设条，连续多次拖拽-松手循环无残留。
- 双指缩放起点在取景框任意位置（含变焦条上）：不弹轮盘或瞬间关闭，倍数徽章消失，预设条恢复。
- 连续点击预设档位（0.6x→2x、2x→0.6x）：中间 1x 不再闪亮，直接高亮目标档位。
- 顶部栏六点菜单与画幅下拉：上方直角、下方圆角，样式与底部栏区分。

### 补充说明（0.1.28 内容并入，本版本已弃用）

- 0.1.27 的「手势层固定全高 + 起点位置判断」方案实测未解决轮盘滞留问题，且导致双指缩放灵敏度下降；变焦模组已按用户要求回退至 0.1.26 模式（手势层恢复「折叠时仅覆盖变焦条、展开时覆盖轮盘区域」的动态高度/偏移布局，移除 `selectedPreset` 预设锁定）。
- 保留：顶部栏二级菜单上方直角/下方圆角样式（非变焦模组）；全屏指针追踪器（0.1.26 引入）。
- 后续以 B 方案（`detectDragGestures` 高级手势托管拖拽生命周期 + `selectedPreset` 预设档位锁定）重新尝试修复轮盘滞留与预设闪烁，成功后再更新版本号。

---

## beta 0.1.26

- 版本代码：126
- 更新日期：2026-08-01

### 核心改进

1. **轮盘松手残留再次修复（全屏指针追踪器兜底）**
   - 0.1.25 的双保险（手势层 finally + 取景框 pinch finally）在单指拖变焦条场景仍偶发失效：轮盘呼出瞬间手势层高度/偏移 relayout，存在丢失手指抬起事件的路径，`finally` 不触发，轮盘停留界面。
   - 新增全屏指针追踪器：`CameraControls` 根 Box 上加布局恒定的 `pointerInput`（`awaitEachGesture` + `try/finally`），任何手势的每一次 down 之后，无论手指正常抬起还是协程被取消，`finally` 必然执行 `showWheel = false`。该追踪器覆盖全屏、布局从不变化，不会丢失抬起事件，独立于手势层与 pinch 处理器，保证轮盘在任何松手路径下必定消失。

### 工程修复

- `CameraControls`：根 Box 新增全屏指针追踪器（`awaitEachGesture`/`awaitFirstDown(requireUnconsumed=false)` + `try/finally` 关闭轮盘）。
- 修正版本代码递增：0.1.25 时 `versionCode` 因并行编辑覆盖未生效（仍为 124），本次恢复递增至 126。
- 版本号升级至 beta 0.1.26（版本代码 126）。

### 测试重点

- 变焦条单指拖拽呼出轮盘后松手：轮盘立即消失、恢复三图标预设条，无需点击；连续多次拖拽-松手循环无残留。
- 变焦条拖拽中快速反向拖动、松手位置在轮盘任意区域，轮盘均正常消失。
- 双指缩放（含起点在变焦条上）后松手：不弹轮盘或瞬间关闭，预设条恢复。
- 底部栏快门/相册点击、滤镜模式各交互不受追踪器影响（追踪器只读不消费事件）。

---

## beta 0.1.25

- 版本代码：125
- 更新日期：2026-08-01

### 核心改进

1. **轮盘变焦松手后轮盘残留修复（根治）**
   - 根因：轮盘呼出瞬间手势层（变焦条/轮盘手势条）高度与偏移发生 relayout，存在丢失手指抬起事件的路径，`finally` 不触发 → 轮盘停留在界面，需再点一下才隐藏。
   - `showWheel` 状态从 `ZoomControl` 内部提升到 `CameraControls`；取景框全屏 pinch 手势的 `finally` 中同时复位 `showWheel`（覆盖全屏、布局稳定，必然收到抬起事件），与手势层 `finally` 形成双保险，任何松手路径轮盘必定消失。

2. **滤镜模式快门位置调整（除"标准"色卡外）**
   - 快门从底部栏移出，改贴在滤镜卡片预览框（3:4 取景框）底部边缘，水平居中于屏幕中轴，与底部栏调色盘居中对齐。
   - 色卡最小化时（显示变焦条）快门自动回到底部栏与调色盘并排，避免遮挡变焦条。

3. **滤镜模式重置/关闭按钮左右互换**
   - 重置按钮（刷新图标）移至右侧，关闭按钮（X 图标）移至左侧。

4. **滤镜强度条样式重做**
   - 新增白色圆形操作杆：直径与轨道高度（两端圆角）一致，轨道内滑动、垂直居中。
   - 底色改为双层：空值（value=0）时为纯黑；有值部分从左侧覆盖当前模式主题色渐变——向右拉渐变色增加，向左拉黑色增加。
   - 边框线条加强（1dp / 白色 40%），与底部栏明显区分。

### 工程修复

- `CameraControls`：提升 `showWheel` 状态，几何层新增滤镜模式（非标准）快门悬浮层，pinch `finally` 复位轮盘。
- `ZoomControl`：`showWheel` 改为受控属性（`showWheel` + `onShowWheelChange`），移除内部自持状态。
- `FilterModeBottomPanel`：新增 `filterCardsMinimized` 参数，重置/关闭按钮左右互换，快门按色卡状态切换显示位置。
- `HorizontalIntensitySlider`：改用 `BoxWithConstraints` 双层轨道 + 白色圆形 thumb + 加强边框。
- 版本号升级至 beta 0.1.25（版本代码 125）。

### 测试重点

- 变焦条拖拽呼出轮盘后松手：轮盘立即消失、恢复三图标预设条，无需额外点击；多轮快速拖拽无残留。
- 双指缩放（含起点在变焦条上）后松手：轮盘不触发或瞬间关闭，倍数徽章消失，预设条恢复。
- 滤镜模式非标准滤镜：快门位于预览框底部居中、与调色盘中轴对齐；色卡最小化后快门回底部栏且变焦条可正常拖动。
- 滤镜模式重置按钮在右侧、关闭按钮在左侧，点击行为正确。
- 强度条：向右拖渐变增加、向左拖黑色增加；白色圆杆随值滑动且不溢出轨道；边框线条清晰可辨。

---

## beta 0.1.24

- 版本代码：124
- 更新日期：2026-08-01

### 核心改进

1. **轮盘变焦触发范围收窄至变焦条内**
   - 原手势层为「覆盖整个取景框（`contentBaselineY` 以上）」的透明条，任何双指缩放的起点（第一指落在取景框任意位置）都会被捕获，第一指移动超过触摸阈值即误弹轮盘。
   - 手势层改为「折叠时仅覆盖变焦条（48dp 高，底部对齐取景框底线）；轮盘呼出后自动扩展覆盖整个轮盘区域」——双指缩放起点在变焦条之外时完全不会触发轮盘，从根源消除「双指缩放误触发轮盘」。
   - 轮盘呼出后手势层扩展至全轮盘区域，用户可在轮盘任意位置继续拖拽旋转。

2. **轮盘/预设条复原逻辑强化**
   - 手势层 `finally` 无条件复位 `showWheel`；多指按压或 pinch 进行中（`pressed >= 2 || currentIsPinching`）立即放弃拖拽并复位，杜绝轮盘残留。
   - 取景框 pinch 处理器 `isPinching` 复位放入 `try/finally`，`awaitEachGesture` 取消 block 时也会执行，松手必定恢复三图标预设条。

3. **消除 `BoxWithConstraints scope is not used` lint 警告（1389 行）**
   - 该警告为静态分析提示，不影响运行时行为；根因为 `maxWidth`/`maxHeight` 在嵌套 `with(density)` lambda 内引用，lint 检测不到。
   - 改为先在 lambda 顶层直接赋值 `val maxW = maxWidth`、`val maxH = maxHeight`，再在 `with` 内转 px，lint 消除。

### 工程修复

- `ZoomControl` 手势层：高度/偏移随 `showWheel` 切换（折叠 = 变焦条高度，展开 = `contentBaselineY`），移除未使用变量 `startY`。
- `CameraControls` 几何层：`maxWidth`/`maxHeight` 顶层直接引用后再转 px。
- 版本号升级至 beta 0.1.24（版本代码 124）。

### 测试重点

- 双指缩放起点在取景框任意位置（非变焦条区域）：不得弹出轮盘，仅显示实时倍数徽章，松手立即恢复预设条。
- 双指缩放起点故意压在变焦条上：轮盘最多闪现一瞬即关闭，pinch 变焦正常。
- 单指在变焦条上左右拖拽：正常呼出轮盘并旋转变焦，松手轮盘立即消失恢复预设条。
- 点击变焦条预设档位（0.6x / 1x / 2x）仍正常。
- 反复快速双指缩放多次循环后无残留（无需额外点击复原）。

---

## beta 0.1.23

- 版本代码：123
- 更新日期：2026-08-01

### 核心改进

1. **双指缩放不再误触发轮盘变焦**
   - `ZoomControl` 手势层原先只跟踪第一根手指：双指捏合时若第一指起点落在变焦条区域，第一指移动超过触摸阈值即被误判为拖拽 → 轮盘弹出，与 pinch 变焦互相打架。
   - 手势层事件循环新增多指检测：`event.changes.count { it.pressed } >= 2` 或 pinch 已开始（`isPinching`）时立即放弃拖拽并复位 `showWheel`，将手势完整交还给取景框 pinch 处理器。

2. **轮盘/徽章松手残留修复（无需再点一下）**
   - 取景框 pinch 处理器中 `isPinching = false` 原位于 `while` 循环之后；`awaitEachGesture` 在最后一指抬起的瞬间会取消整个 block，若取消先于循环正常 break，该行被跳过 → `isPinching` 卡死为 true，三图标预设条不再恢复，只能再点一次屏幕才复原。
   - 改为 `try/finally` 包裹事件循环，`isPinching = false` 在 finally 中无条件执行，任何松手/取消路径都能恢复预设条。

### 工程修复

- `CameraControls` 取景框 pinch 手势：事件循环外包 `try/finally`，`isPinching` 复位移入 `finally`。
- `ZoomControl` 手势层：循环开头检测多指（`pressed >= 2`）与 `currentIsPinching`，满足即退出拖拽且不执行预设点按逻辑，杜绝幻影预设跳变。
- 版本号升级至 beta 0.1.23（版本代码 123）。

### 测试重点

- 双指捏合起点落在变焦条/轮盘区域时，不应弹出轮盘，只显示实时倍数徽章；松手立即恢复三图标预设条。
- 轮盘拖拽单指操作不受影响：左拖放大、右拖缩小，松手轮盘立即消失。
- 快速双指缩放（手指先后落下、快速抬起）多次循环后，预设条与倍数徽章无残留，无需额外点击。
- 点击变焦条预设档位仍正常切换 0.6x / 1x / 2x。

---

## beta 0.1.22

- 版本代码：122
- 更新日期：2026-08-01

### 核心改进

1. **轮盘样式最终定版**
   - 轮盘为「圆形 + 矩形」组合：圆形直径 = 矩形宽幅 = 底部栏宽幅（全屏宽），圆形与矩形均为纯黑填充。
   - 绘制层级：圆形刻度盘与刻度/标签在下层，矩形在上层，掩盖圆盘下半部分与整个底部栏。
   - 圆形取消白色边缘线，仅保留量角器刻度线与数字标签。

2. **打开滤镜模式卡死修复**
   - `awaitEachGesture` 内部自带循环，若回调首句无挂起点直接 `return` 会造成无限空循环占满主线程。
   - 取景框 pinch 手势与 `ZoomControl` 改为先 `awaitFirstDown()` 再判断条件，移除 `ZoomControl` 多余的外层 `while(true)`。

3. **默认模式快门/相册点不动修复**
   - 拖拽变焦手势结束 `finally` 无条件复位 `showWheel`，杜绝轮盘残留时纯黑矩形盖住底部栏并拦截点击。
   - `ZoomControl` 拆分视觉层与手势层：手势层为透明窄条，仅覆盖变焦条与轮盘区域（`contentBaselineY` 以上），不再覆盖底部栏；视觉层（变焦条/轮盘）无手势处理器，不参与点击拦截。底部栏快门/相册在任何状态下都可点击。

4. **双指缩放触发修复**
   - 取景框 pinch 手势的 `awaitFirstDown` 改为 `requireUnconsumed = false`，避免点击对焦检测器先消费 down 导致 pinch 永远无法开始。

5. **消除 Compose Lint 警告**
   - `CameraControls` 几何层 `BoxWithConstraints` 改用 scope 直接成员 `maxWidth`/`maxHeight`，消除 `BoxWithConstraints scope is not used` 警告。

### 工程修复

- `ZoomWheel` 绘制顺序调整：圆盘 + 刻度/标签在下层，矩形在上层掩盖下半；圆形半径改为组件宽度一半（直径 = 矩形宽幅 = 底部栏宽幅）；删除白色圆弧轨道（drawArc）。
- `ZoomControl` 拆分为视觉层（变焦条/轮盘，无手势）与手势层（透明窄条，高度 = `contentBaselineY`）；手势层 `finally` 无条件复位 `showWheel`。
- `CameraControls` 取景框 pinch 手势 `awaitFirstDown(requireUnconsumed = false)`，保证双指缩放可触发。
- `CameraControls` 几何层 `BoxWithConstraints` 改用 `maxWidth.toPx()` / `maxHeight.toPx()`。
- 版本号升级至 beta 0.1.22（版本代码 122）。

### 测试重点

- 打开/关闭滤镜模式是否流畅不卡死；滤镜模式下预览与色卡交互是否正常。
- 默认模式下快门/相册是否可正常点击（直接打开软件即可点，拖拽变焦松手后轮盘立即消失、底部栏恢复可点）。
- 双指缩放是否可正常触发变焦（取景框内双指捏合放大/缩小）。
- 轮盘区域是否为纯黑底（圆盘 + 下半矩形遮罩），无白色边缘线；圆形直径是否与底部栏同宽。
- 拖拽轮盘/双手缩放松手后是否立即恢复三图标变焦条，无残留轮盘或单个倍数徽章。

---

## beta 0.1.21

- 版本代码：121
- 更新日期：2026-08-01

### 核心改进

1. **对焦框交互跟手性修复**
   - 使用 `rememberUpdatedState` 包装 `cameraControl`、`previewSize` 与 `isFocusLocked`。
   - `pointerInput` 的 key 固定为 `Unit`，避免相机状态变化导致手势检测器重建，从而解决对焦框偶发失效/不跟手的问题。

2. **调色盘实际阈值降低**
   - UI 仍显示 `-100 ~ +100`，但实际效果应用缩放系数：饱和度 `0.5`、亮度 `0.35`。
   - 避免过度调整导致画面失真或色阶裁切，保持精细调节手感。

3. **顶部信息条顺序调整**
   - 滤镜模式顶部实时信息条顺序改为「亮度 ±N   饱和度 ±N   强度 N」。

4. **变焦平滑动画**
   - 点击变焦条预设（如 `1x` → `2x`）时，使用 `Animatable` + `tween(200)` 同步动画 UI 与相机变焦比例。
   - 消除之前的硬切/跳变，变焦过渡更加丝滑。

5. **轮盘变焦样式重构**
   - 轮盘改为「圆形刻度盘 + 下半圆矩形遮罩」组合，圆盘半透明黑填充、矩形半透明黑遮罩，矩形盖住圆盘下半部分与底部栏。
   - 刻度盘随变焦旋转：放大时逆时针旋转、缩小时顺时针旋转，与变焦条手势（左滑放大、右滑缩小）方向一致。
   - 上半圆为量角器式刻度，从左至右顺时针依次标注 `0.6x`、`1x`、`2x`、`3.2x`、`5x`、`10x`。
   - 数字标签径向对准圆心（钟表式排列，数字底边朝向圆心）。
   - 圆盘顶部中央固定黄色指针，实时倍数徽章（明黄色）固定在指针上方显示。
   - 轮盘缩放范围固定为 `0.6x ~ 10x`，刻度角度与实际变焦倍数通过对数映射精确对应。

6. **变焦条位置与交互修复**
   - 预设条底边精确落在取景框底边（1:1 / 3:4）或 3:4 画幅底边线（9:16 等长画幅），向上伸入取景框内。
   - 手势检测区与实际条位置一致，点击/拖拽预设可正常响应；命中变焦手势区后 `down.consume()`，点击预设不再同时触发对焦框。
   - 修复长画幅下变焦条位置：`p4_3Top` 计算补上取景框上移偏移，与 3:4 画幅时位置完全一致。

7. **松手即恢复**
   - 双手缩放手势结束强制复位 `isPinching`，松手立即恢复三图标变焦条，不再残留单个倍数徽章或轮盘。

8. **双手缩放放大失控/画面卡死修复**
   - pinch 起始时固定 `baseZoom`，手势内使用累计缩放因子，避免与实时 UI 值复合叠加导致倍数飙升、画面异常。

9. **预览残影/卡顿修复**
   - `CameraFrameRenderer` 改为三缓冲 `FramePlanes`，分析线程只写入空闲缓冲、GL 线程只上传待渲染缓冲，消除同一 YUV 缓冲区并发读写导致的撕裂（移动画面时表现为残影与色差）。

10. **顶部栏二级菜单优化**
    - 六点菜单/画幅下拉改为从顶部栏顶部向下延伸、完全遮住顶部栏。
    - 移除全屏半透明黑色遮罩，只保留菜单卡片本身，点击卡片外区域关闭。

### 工程修复

- `ViewfinderTapHandler` 使用 `rememberUpdatedState` 稳定手势检测器，修复对焦框偶发失效。
- `CameraViewModel` 增加 `SATURATION_EFFECT_SCALE`（0.5）与 `BRIGHTNESS_EFFECT_SCALE`（0.35），调色盘 UI 保持 ±100 而实际效果减半以下。
- `CameraContent` 新增 `animateZoomTo`，使用 `Animatable.animateTo(tween(200))` 同步动画 UI 与相机变焦。
- `CameraControls` 取景框手势改为手动 `awaitEachGesture`，每次手势结束强制 `isPinching = false`，并使用固定 `baseZoom` 避免缩放复合放大。
- `ZoomWheel` 重构为旋转圆形刻度盘 + 下半圆矩形遮罩；旋转方向取反（放大逆时针）；标签旋转角改为 `90f - thetaDeg` 实现径向对齐；圆盘补半透明黑填充。
- `ZoomControl` 手势区域限制为预设条附近，避免与底部快门/调色盘冲突；预设条改为 48dp 容器 `BottomCenter` 对齐，底边与手势区一致。
- `CameraFrameRenderer` 改为三缓冲 `FramePlanes` 消除缓冲竞争，丢帧时仍触发 `requestRender` 防止渲染饿死。
- 长画幅变焦条位置 `p4_3Top` 补 `- viewfinderOffsetPx`。
- `SixDotMenu` 移除全屏半透明遮罩；六点菜单与画幅下拉卡片从状态栏下方（顶部栏顶部）向下延伸。
- 版本号升级至 beta 0.1.21（版本代码 121）。

### 测试重点

- 反复点击/长按取景框不同位置，对焦框是否每次都能出现且跟随手指。
- 调色盘拖到 ±100 时，实际画面饱和度/亮度是否不会过度失真。
- 顶部信息条顺序是否为亮度 → 饱和度 → 强度。
- 点击变焦条 `1x` / `2x` 预设，变焦是否以约 200ms 平滑过渡，而非硬切。
- 轮盘左拖放大时是否逆时针旋转、右拖缩小时顺时针旋转；数字底边是否朝向圆心。
- 1:1 / 3:4 画幅下变焦条底边是否紧贴取景框底部，9:16 长画幅下是否与 3:4 画幅位于同一高度。
- 点击变焦预设时是否只变焦、不会同时触发对焦框。
- 双手缩放变焦是否平滑、不会放大失控或画面卡死。
- 快速移动相机（尤其滤镜模式下）预览是否不再出现残影/色差/卡顿。
- 六点菜单/画幅下拉是否从顶部栏顶部向下延伸并遮住顶部栏，无全屏半透明遮罩。

---

## beta 0.1.20

- 版本代码：120
- 更新日期：2026-08-01

### 核心改进

1. **调色盘与快门统一布局**
   - 调色盘尺寸从 128 dp 缩小到 100 dp，避免与快门重叠。
   - 滤镜模式快门从取景框底部移到底部栏，与调色盘作为同级组件并排显示。
   - 默认模式与滤镜模式使用完全相同的快门组件与尺寸（76 dp），保持视觉统一。

2. **预览框位置上移与滤镜画幅锁定**
   - 取景框整体上移 24 dp，为底部栏腾出更多空间。
   - 滤镜模式强制锁定为 3:4 画幅，切换滤镜或显示/隐藏色卡时布局保持稳定。

3. **滤镜玻璃卡片去色**
   - 滤镜卡片背景从主题色渐变改为无色磨砂玻璃（中性白/黑透明度）。
   - 滤镜之间的区别完全由画面风格化效果体现，避免玻璃颜色对画面产生干扰。

4. **调色盘数值连续化**
   - 饱和度与亮度数值范围改为 -100 到 +100 连续可调。
   - 11×11 点阵仅保留视觉吸附效果，实际数值不再强制按档位划分，可设定如 99 等精细数值。

5. **滤镜强度条与顶部信息条增强**
   - 水平强度条增加与调色盘一致的主题色 HSL 渐变底色。
   - 滤镜模式顶部实时信息条新增「强度 0~100」显示，格式为「强度 N   亮度 ±N   饱和度 ±N」。

### 工程修复

- `CameraViewModel` 移除 `snapToGrid` 对实际数值的吸附，`_squarePanelOffset` 存储连续偏移，`saturation`/`brightness` 直接映射到 ±100。
- `SquareColorPanel` 拖拽/点击回调改为上报连续偏移，绘制时仍按 11×11 网格进行视觉吸附。
- `FilterCardPager` 背景色改为中性白透明渐变，移除 `filter.themeColor` 对玻璃卡片的影响。
- `HorizontalIntensitySlider` 新增 `themeColor` 参数，轨道使用 HSL 主题色渐变，进度覆盖层保持白色半透明。
- `CameraControls` 取景框应用 24 dp 向上偏移，并同步更新 `ViewfinderOverlay` 与统一几何层的坐标计算。
- `FilterModeBottomPanel` 重构为 [重置] [调色盘+强度条+快门] [关闭] 布局，标准滤镜下仅保留快门/重置/关闭。
- `CameraContent` 增加 `LaunchedEffect(filterSelectionMode)`，进入滤镜模式时自动将画幅设为 3:4。
- 版本号升级至 beta 0.1.20（版本代码 120）。

### 测试重点

- 滤镜模式下调色盘是否与快门并排显示、无重叠。
- 默认模式与滤镜模式快门样式、尺寸是否一致。
- 取景框是否整体上移，顶部栏/底部栏组件是否仍保持固定可见。
- 进入滤镜模式后画幅是否强制为 3:4，且无法切换其他画幅。
- 滤镜卡片玻璃是否不再带主题色，仅呈现透明磨砂效果。
- 调色盘拖拽时数值是否可在 -100~+100 之间连续变化（如 99），点阵是否仍为视觉吸附。
- 顶部信息条是否显示强度、亮度、饱和度三项。
- 强度条底色是否随当前滤镜主题色变化。

---

## beta 0.1.19

- 版本代码：119
- 更新日期：2026-07-30

### 核心改进

1. **调色盘视觉重构**
   - 调色盘分为圆角底色区块与内部点阵区域，主题色渐变现在完整填充至四角圆角，不再只覆盖方形点阵区。
   - 点阵样式对齐 HTML 参考：11×11 网格、圆点大小为格子的 30%、中心为空心圆环。
   - 静止时显示弱化十字轴线（透明度 0.65），拖拽时切换为以摇杆为中心的两档荧光光斑（距离 1/2），并保留光晕扩散效果。
   - 摇杆拖动时放大至约 3.8 倍，释放后平滑回弹。

2. **变焦条与轮盘修复**
   - 拖拽变焦松手后轮盘立即收起，恢复三图标预设条。
   - 轮盘仅显示上半圆，下半圆始终被裁剪，不受画幅影响。
   - 双指缩放时的实时倍数徽章改为独立悬浮层，固定在取景框底部居中，不再随 ZoomControl 内部切换而跳动。
   - 双指松手后徽章消失并恢复默认三图标变焦条。
   - 变焦条位置策略调整：1:1 与 3:4 画幅时紧贴实际取景框底部居中；9:16 等长画幅时固定在与 3:4 相同的底部高度，避免与底部控制重叠。

3. **滤镜模式按钮层级修复**
   - 色卡最小化/放大按钮从滤镜卡片层中抽出，单独渲染在变焦条上方。
   - 修复隐藏色卡后点击按钮无响应或误触 2x 变焦的问题。

4. **顶部栏与底部栏纯黑固定**
   - 顶部栏、底部栏及二级菜单背景改为纯黑色，取消透明度。
   - 所有控制组件悬浮在黑色背景区块上方固定位置，切换长画幅时只会挤压背景区块大小，不会挤压组件。
   - 修复 9:16 等长画幅下顶部栏被挤出屏幕上方的问题。

### 工程修复

- `SquareColorPanel` 移除内部 `padding` 裁剪，改为外层圆角底色区块 + 内层带 `gridPadding` 点阵，主题渐变在 Canvas 全尺寸绘制以填满圆角。
- `CameraControls` 引入统一几何层 `BoxWithConstraints`，同时计算取景框边界、变焦条底部 Y、 pinch 徽章位置与滤镜最小化按钮位置。
- `ZoomControl` 不再内部渲染 pinch 徽章，仅负责预设条与轮盘；徽章由 `CameraControls` 固定渲染。
- `FilterCardsOverlay` 移除右下角按钮，按钮改在 `CameraControls` 中独立定位，确保点击层级高于变焦条。
- 画幅下拉菜单背景从 `Black @ 55%` 改为纯黑。

### 测试重点

- 调色盘四角圆角处是否能看到主题渐变底色，而非黑色。
- 点阵默认十字轴线、拖拽光斑、摇杆放大是否符合 HTML 样式。
- 左右拖拽变焦条时轮盘是否为半圆，松手是否立即消失。
- 双指捏合时底部徽章是否固定居中，松手是否恢复三图标。
- 滤镜模式下隐藏色卡后，右下角放大按钮是否可正常点击，不会误触变焦。
- 切换为 9:16 长画幅后顶部栏组件是否仍可见，不被挤出屏幕。
- 变焦条在 1:1、3:4 与 9:16 画幅下的位置是否符合预期。

---

## beta 0.1.18

- 版本代码：118
- 更新日期：2026-07-30

### 核心改进

1. **调色盘交互与视觉修复**
   - 修复调色杆偏移问题：指针坐标现在正确扣除了 Canvas 内边距，摇杆会精确跟随手指移动。
   - 确认 Y 轴方向：向上拖动为提亮、向下拖动为压暗，与亮度档位标签一致。
   - 保留 25 dp 圆角，通过内边距控制点阵比例，确保四角圆点不被圆角裁剪。

2. **变焦条与轮盘重构**
   - 将拖拽/点击手势统一到 `ZoomControl` 层级，避免轮盘出现后无法接管手势的问题。
   - 手指在变焦条上左右拖动时，预设条立即切换为半圆轮盘，轮盘刻度随当前倍数实时旋转。
   - 松手后轮盘立即收起并恢复三图标预设条，无残留状态。
   - 变焦条始终位于取景框底部，不受画幅切换影响。
   - 双指捏合时底部直接由三图标切换为单枚实时倍数徽章（无聚合动画），松手恢复。

3. **1x 预设点击变蓝问题**
   - 点击预设图标时，UI 与相机变焦同步立即到位，不再通过 `tween` 动画制造 UI/相机状态差，避免触发传感器/AWB 调整导致的短暂偏色。

4. **前后摄像头切换遮罩**
   - 遮罩在点击切换瞬间几乎立即出现（淡入 50ms），与切换过程并行。
   - 遮罩保持 700ms 以完整覆盖相机重新绑定和首帧稳定，避免看到短暂定格或翻转。

5. **对焦框动画调整**
   - 对焦框默认尺寸从 72 dp 增大到 80 dp。
   - 点击/长按时对焦框以放大的初始状态出现并平滑回弹至正常尺寸，动画阻尼加大，减少突兀感。

### 工程修复

- `SquareColorPanel` 引入 `gridPaddingPx`，拖拽与点击坐标均按实际内边距换算。
- `ZoomControl` 统一处理手势状态，`ZoomPresetBar` 与 `ZoomWheel` 改为纯视觉组件。
- `animateZoomTo` 改为直接调用 `handleZoomChange`，消除快速变焦时的色彩闪烁。
- `LensSwitchMask` 淡入加速至 50ms，绑定完成后延迟从 450ms 延长至 700ms。
- `FocusIndicator` 与 `FocusExposureUI` 的框尺寸统一为 80 dp，动画阻尼调整为 0.75。

### 测试重点

- 调色盘拖动时摇杆是否精确位于手指下方，Y 轴方向是否正确。
- 调色盘四角圆点是否完整、圆角是否可见。
- 变焦条左右拖拽时轮盘是否即时出现并跟随手指旋转，松手是否立即收起。
- 双指捏合时底部是否只显示一个黄色实时倍数徽章。
- 点击 1x 预设是否不再出现短暂变蓝。
- 切换前后摄像头时遮罩是否立即出现并持续到切换完成。
- 对焦框是否稍大、回弹动画是否自然。

---

## beta 0.1.17

- 版本代码：117
- 更新日期：2026-07-30

### 核心改进

1. **变焦轮盘交互优化**
   - 轮盘支持水平拖拽变焦：向左拖拽放大、向右拖拽缩小，采用线性灵敏度，响应更直接。
   - 手指离开轮盘后，轮盘立即收起，不再延迟。
   - 轮盘下半部分始终被底部栏遮挡，仅显示上半圆。
   - 双指捏合变焦时，底部三枚预设标签通过缩放聚合为单个实时倍数徽章，而不是滑动切换。
   - 底部变焦条向左拖拽即可连续放大，灵敏度提升。

2. **调色盘点阵修复**
   - 进一步确认并保证十字高亮与调色杆位置同步。
   - 调色杆始终吸附在 11×11 点阵圆心，不会随意停靠。
   - 增大点阵内边距并缩小圆点比例，避免四角被圆角遮挡。
   - 摇杆尺寸与光晕比例同步微调，视觉更协调。

3. **取景框位置固定**
   - `CameraControls` 根布局改为 `Column`，顶部栏、取景框、底部栏三区高度固定。
   - 打开/关闭滤镜或显示/隐藏色卡时，取景框区域不再上下浮动。

4. **顶部栏二级菜单一体化**
   - 六点菜单与画幅下拉菜单从顶部栏直接延伸，去除额外顶部间距与阴影。
   - 菜单宽度与顶部栏对齐，背景色保持 `Black @ 55%`，增强一体感。

5. **镜头切换遮罩动画延长**
   - 遮罩显示时间延长至 1200ms，淡入/淡出更柔和。
   - 遮罩中央增加翻转的镜头切换图标（旋转 + 缩放），避免纯黑等待感。

6. **对焦框动画与交互**
   - 点击/长按预览框时，对焦框先放大至 1.5 倍再回弹到正常尺寸。
   - 锁定对焦时对焦框保持明黄色；点击其他区域解锁后恢复普通白色并自动消失。

7. **六点菜单图标居中**
   - 顶部栏六点菜单按钮的图标尺寸调整为 22dp，确保在 40dp 按钮内视觉居中。

### 工程修复

- `CameraControls` 移除 pinch 结束后的 400ms 延迟任务，手指离开即恢复预设条。
- `ZoomPresetBar` 使用 `AnimatedContent` 实现三标签向中心聚合的捏合动画。
- `SquareColorPanel` 调整 `gridPadding`、`dotRadius`、`handleRadius` 以优化点阵比例。

### 测试重点

- 轮盘水平拖拽变焦是否顺滑、松手是否立即消失、下半部分是否被底部栏遮挡。
- 双指捏合时底部三标签是否向中心聚合为单徽章，松手后是否恢复三标签。
- 取景框在默认/滤镜/隐藏色卡切换时是否保持固定。
- 调色盘点阵四角是否完整显示、摇杆是否吸附到圆心。
- 顶部菜单是否与顶部栏无缝衔接。
- 前后摄像头切换遮罩是否覆盖完整切换过程并显示翻转图标。

---

## beta 0.1.16

- 版本代码：116
- 更新日期：2026-07-30

### 核心改进

1. **调色盘复刻 HTML 点阵风格**
   - 点阵从 44×44 改为 11×11，圆点大小为格子的 30%，居中排列。
   - 中心点改为空心圆环，默认透明度 0.3，选中时 0.9。
   - 当前摇杆所在行/列的点阵高亮为 0.9 透明度，形成十字坐标轴反馈。
   - 保留滤镜主题色的 HSL 渐变背景，白色点阵叠加上方。

2. **调色盘档位化**
   - X 轴（饱和度）与 Y 轴（亮度）各分为 11 档：+5 ~ -5。
   - 拖动/点击时摇杆吸附到最近的格子中心。
   - 内部阈值保持原有 0.35 / 0.25 系数不变，仅 UI 显示为 11 档标签。

3. **摇杆拖动光晕效果**
   - 静止时摇杆为白色小圆。
   - 拖动时通过 `Animatable` 放大 2.2 倍，并叠加两层白色光晕（0.17 / 0.09 透明度）。

4. **滤镜强度条改为水平**
   - 强度条从调色盘右侧移到调色盘正下方。
   - 调色盘 + 强度条整体在底部栏中水平居中。
   - 调色盘尺寸从 72dp 放大到 128dp，强度条宽度与调色盘一致。

5. **滤镜模式顶部栏调整**
   - 滤镜模式下隐藏画幅、闪光灯、镜头切换按钮，仅保留六点菜单。
   - 顶部中心增加实时信息条，显示当前「亮度 ±N   饱和度 ±N」。

6. **色卡缩放按钮固定**
   - 放大/缩小按钮固定在取景框右下角，不再随色卡滑动动画移动。
   - 仅滤镜卡片玻璃层参与上滑/下滑切换动画。

### 工程修复

- 修复 `CameraControls` 花括号不匹配导致的编译错误。
- `CameraViewModel` 增加 `snapToGrid` 与档位标签计算。
- `SquareColorPanel` 使用 `rememberCoroutineScope` 驱动摇杆缩放动画。

### 测试重点

- 11×11 点阵是否清晰、十字高亮是否跟随摇杆。
- 拖动调色盘时是否明显吸附到 11 个档位。
- 顶部信息条是否实时显示亮度/饱和度档位。
- 滤镜强度条是否在调色盘下方、宽度一致。
- 色卡显示/隐藏时右下角按钮是否保持固定。
- 滤镜模式下顶部栏是否只剩六点菜单。

---

## beta 0.1.15

- 版本代码：115
- 更新日期：2026-07-29

### 核心改进

1. **底部变焦条重构 — 三图标 + 右滑半圆轮盘**
   - 移除右侧垂直滑动变焦条。
   - 底部常驻 0.6x / 1x / 2x 三枚圆角图标。
   - 向右滑动图标区域展开半圆形 `ZoomWheel` 轮盘，支持无级变焦。
   - 超过 2x 时最右侧图标实时显示实际倍数（如 3.2x）。
   - 轮盘 1.5s 无操作后自动收起。

2. **双指捏合变焦**
   - 取景框区域新增捏合手势识别。
   - 捏合过程中不显示轮盘，底部收缩为单个实时倍数徽章。
   - 捏合结束后 400ms 恢复三图标显示。

3. **前后摄像头切换优化与自拍方向修复**
   - 前置镜头时反转 `rotationDegrees`，修复画面上下颠倒。
   - 切换镜头时触发 80ms 淡入 / 220ms 淡出的黑屏遮罩，屏蔽切换瞬间卡顿与翻转。
   - 切换前重置 zoom 到 1x，减少状态抖动。

4. **滤镜算法替换为简单颜色滤镜**
   - 彻底移除 `LutGenerator` 与 3D LUT 纹理采样，消除 LUT 导致的红色纹理/色边。
   - `CameraFrameRenderer` / `PhotoRenderer` 片元着色器内直接实现曝光、亮度、对比度、黑阶、高光/阴影、饱和度、色温/色调/暖度、色相偏移、暗角等调整。
   - 输入/输出均钳制到 `[0, 1]`，避免通道溢出。
   - 默认模式与「标准」滤镜继续不加载 LUT，保持画面干净。

### 工程修复

- `CameraScreen.kt` 编译错误修复：新增 `detectVerticalDragGestures` 导入、提升 `isPinching` 作用域、提取 `LensSwitchMask` 消除 `ColumnScope` 歧义、修复 `drawArc` 的 `Size` 类型冲突。

### 测试重点

- 底部三图标常驻、右滑展开轮盘、超过 2x 图标变化。
- 双指捏合变焦、底部单徽章显示。
- 切换镜头黑屏遮罩、前置画面方向。
- 各滤镜是否仍有红色纹理，成片与预览是否一致。

---

## beta 0.1.14

- 版本代码：114
- 更新日期：2026-07-29

### 核心改进

1. **渲染管线优化**：`CameraFrameRenderer` 增加按需渲染回调，`RealTimeCameraView` 使用 `RENDERMODE_WHEN_DIRTY`，减少拖影；UV 通道改回 `GL_NEAREST`。
2. **9:16 画幅顶部栏固定**：`CameraControls` 根布局改为 `Column + weight(1f)`，`TopControlBar` 用 `Box` 绝对定位顶部。
3. **滤镜模式按钮布局重构**：关闭/重置按钮与调色盘水平齐平；最小化/放大按钮统一在取景框右下角。
4. **滤镜模式快门位置调整**：快门悬浮于取景框底部居中、底部栏上方。
5. **六点二级菜单简化**：移除下滑关闭和取消按钮，点击外部关闭，顶部增加向下箭头。
6. **LUT 红色纹理修复与滤镜卡片玻璃化**：降低温度/色调/暖度强度，增加 `clamp3`，卡片改为无边玻璃样式。
7. **滤镜模式支持对焦框与曝光补偿**：`FocusExposureUI` 覆盖取景框层级。
8. **变焦条在滤镜模式最小化色卡后显示**：显示条件改为 `!filterSelectionMode || filterCardsMinimized`。
9. **色卡缩小/放大动画**：使用 `AnimatedVisibility` 控制全屏与最小化状态，带滑入滑出动画。
10. **变焦条点击流畅缩放动画**：新增 `zoomAnimatable`，点击预设时用 spring 动画过渡。
11. **前后摄像头切换优化**：统一使用 `imageProxy.imageInfo.rotationDegrees`，`CameraFrameRenderer` 根据 `isMirrored` 更新坐标。

---

## beta 0.1.13

- 版本代码：113
- 更新日期：2026-07-29

### 核心改进

1. **取景器/实景显示范围重构**：实景仅显示在取景框内，主页其余区域纯黑；滤镜模式取景框外覆盖半透明黑色（alpha 0.55）。
2. **滤镜模式按钮重新设计**：移除下滑关闭手势，左下角缩小、右下角关闭，缩小/放大按钮统一为 40dp 圆形。
3. **主界面常驻变焦条**：默认模式底部新增 0.6x / 1x / 2x 预设条，进入滤镜模式自动隐藏。
4. **Style 按钮选中态**：滤镜开启时「Style」显示黄色边框/背景，再次点击关闭滤镜模式。
5. **调色板阈值降低**：饱和度系数从 0.8 降至 0.35，亮度系数从 0.6 降至 0.25。
6. **滤镜红色纹理修复**：LUT 查找前对输入颜色执行 `clamp(0.0, 1.0)`，LUT 采样从 `GL_NEAREST` 改为 `GL_LINEAR`。
7. **成片与取景框一致性**：`ImageCapture` 新增 `ResolutionSelector`，优先选择 4:3 最大输出分辨率并按当前画幅中心裁剪。

---

## beta 0.1.12

- 版本代码：112
- 更新日期：2026-07-29

### 核心改进

1. **滤镜色卡支持缩小与放大**：滤镜模式为覆盖在默认相机 UI 上的全屏卡片层，右下角缩小按钮收缩为浮动小卡片，点击恢复全屏。
2. **滤镜默认选中"标准原图"**：首次打开位于「标准」，关闭后恢复标准原图。
3. **滤镜卡片切换优化**：取消闪弹/回弹动画，使用 `currentPage` 同步状态，避免双向绑定抖动。
4. **菜单手势跟手性**：六点菜单与画幅下拉使用 `DraggableState` 实现垂直拖拽。

### 滤镜分类

- **左侧标准区**：玫瑰金、琥珀色、金色、冷调玫瑰、中性。
- **右侧心情区**：鲜明、反差色、飘渺、温馨、热烈、浪漫、柔和反差、高对比黑白、珠光色。

---

## beta 0.1.10

- 版本代码：110
- 更新日期：2026-07-29

### 核心改进

1. **画质与成片流程重构**：保存照片改用 CameraX 主摄像头最高分辨率，按当前画幅中心裁剪；读取 EXIF 方向旋转；JPEG 质量 100。
2. **专业 LUT 滤镜与调色板联动**：升级为 32³ 3D LUT，实时预览与照片后处理共用同一管线；调色板仅调整饱和度与亮度偏移。
3. **轮盘变焦**：新增右侧纵向轮盘变焦条，支持捏合与轮盘拖动，1.5s 自动隐藏。
4. **UI/UX 重构**：交互组件默认白色、激活态黄色；六点菜单移除标题，滤镜入口用 "Style" 字母图标；滤镜卡片无边框玻璃质感。
5. **功能调整**：移除 HDR；辅助构图线默认关闭并放入六点菜单；默认模式不挂载「标准」滤镜。
6. **取景器布局重构**：统一「顶部栏 / 取景框 / 底部栏」三区块布局，切换画幅不挤压底部栏。
7. **滤镜渲染修复**：默认/标准滤镜 LUT 混合值为 0，避免红色线条；LUT 采样使用 `GL_NEAREST`。
8. **渲染性能**：OpenGL 复用 YUV Buffer，精简着色器，GLSurfaceView 持续渲染。
